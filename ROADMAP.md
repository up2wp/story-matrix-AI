# Story Matrix AI 路线图

## 当前进行中

- 视觉资产真实环境验证：已完成本地与 Immich 双存储契约、生成前 Immich readiness gate、相册映射、统一后端代理展示、Immich 缩略图就绪后再展示成功结果、原图预览代理、图片资产删除及跨记录引用保护、章节人物/道具/服饰候选拆分、章节人物候选共享、候选缓存刷新、服务端重建白底素材草稿上下文、作品视觉资产真实 Provider prompt 展示、独立 `/imagegen` 生图测试台、测试历史持久化、测试历史服务端分页与提示词/创建时间筛选、测试历史单项删除、批量删除和按历史记录再次生成、测试台持久化生成中状态和并发请求、测试台长任务持续轮询刷新、测试资产 owner-scoped 代理、测试台参考图生成时提交、按内容 hash 去重持久化、紧凑参考图上传和历史详情参考图展示、旧数据卷自动迁移兼容、作品生图与测试台共享失败记录、普通用户非超时失败自动关闭生图授权、旧数据卷风控索引迁移顺序兼容，以及 provider 级生图配置基础、OpenAI/OpenAI 兼容与 MiniMax adapter、管理员模型发现和保存后参考图能力保留、模型级请求超时配置生效、OpenAI Images 流式响应解析、provider 网关超时分类、`/imagegen` historyId 关联诊断日志、长生图请求承载层超时放宽、外部 provider 调用安全日志、Immich 上传跨用户隔离。后续重点是用真实 Immich 与真实生图模型做人工验证，并基于测试台反馈优化管理后台配置体验和成本管理。

## 已完成

- 用户级主题偏好已接入：登录用户可在顶部栏保存 `system` / `light` / `dark` 三态偏好，刷新、重新登录和跨设备访问沿用服务端设置；未登录和退出后回到跟随系统，Ant Design 组件与应用壳层支持亮暗主题可读。
- 视觉资产章节候选提取拆分为人物抽取、角色映射、道具抽取和服饰抽取；高清头像与章节服饰共享同一章节人物候选，所有 AI 提取下拉支持章节级缓存和刷新，章节服饰不再自动推断兜底候选，改由用户显式选择“无描述”。
- 视觉资产工作台支持作品级素材生成路径：先选图片类型、再选章节；支持角色高清面部特写、章节服饰、章节道具、多视角全身图，章节道具不再要求选择角色，多视角全身图支持方向选择和模型能力门控下的参考图生成。
- 视觉资产提示词与候选上下文收敛为白底一致性素材；章节提取到的作品角色和未关联人物都会进入下拉候选并参与提示词上下文，未关联人物不会因缺少作品角色 ID 被丢弃，也不会自动写入角色库。
- 生图能力支持管理员配置 provider 和模型，使用独立于小说导入的生图总开关，并继续通过用户级功能权限授权普通用户；管理员保存总开关或用户授权变更后，Sidebar 会立即刷新“视觉资产”的显示状态；服务端 provider adapter 覆盖 OpenAI Images、OpenAI 兼容路径和 MiniMax 中国 `image_generation` 文生图，图片生成走服务端代理并保存为内部资产引用。
- 生图失败风控已收敛到共享后端服务：普通用户在 24 小时窗口内跨作品视觉资产与 `/imagegen` 测试台累计两次非超时失败后，系统会自动关闭该用户的 `imageGeneration` 授权；超时失败不计入，owner/admin 不自动关闭；管理员通过现有用户级功能权限重新授权时会写入恢复基线，普通用户配置响应不暴露风控元数据。
- 视觉资产存储支持默认本地和可选 Immich 模式；Immich 模式按项目名称自动创建或复用相册，生成前通过 readiness gate 验证服务、认证和上传能力，图片通过后端代理读取。
- Docker 构建工作流与测试部署脚本支持通过 `NTFY_URL` / `NTFY_TOPIC` 发送完成或失败通知；本地部署脚本在无新提交跳过构建时不发送通知。

## 执行约束

- 所有生图能力必须走服务端代理，浏览器不暴露上游 API Key、真实 provider URL 或自定义 Header。
- 章节相关提示词生成默认必须使用小窗口、摘要或结构化上下文，禁止单次请求发送或接收整章正文；视觉资产章节候选抽取和草稿生成是例外，可由服务端读取完整章节并发送给系统配置的文本 AI Provider，但浏览器请求、API 响应、作品 JSON、视觉资产缓存、提示词记录、图片记录和日志不得保存或回传完整章节正文。
- 图片二进制不写入作品 JSON；作品状态只保存提示词、模型快照、生成状态和内部资产引用。
- Immich 服务地址、API Key 和远端读取细节只保存在服务端配置中；普通用户和浏览器只能访问 Story Matrix AI 的同源代理地址。
- Immich 模式必须先通过 readiness gate 再调用生图 Provider；Provider 成功但上传失败时最多自动重试 3 次，全部失败后进入可恢复状态。
- V1 不做额度统计、人脸一致性评分、批量重绘、自动挑图或角色头像/章节插图自动写入。
