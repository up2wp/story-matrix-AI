# Story Matrix AI 路线图

## 当前进行中

- 视觉资产真实环境验证：已完成本地与 Immich 双存储契约、生成前 Immich readiness gate、相册映射、统一后端代理展示、章节服饰/道具拆分，以及 provider 级生图配置基础、OpenAI/OpenAI 兼容与 MiniMax adapter、管理员模型发现代理。后续重点是用真实 Immich 与真实生图模型做人工验证，并基于反馈优化管理后台配置体验、图库治理和成本管理。

## 已完成

- 2026-07-05：修复视觉资产章节候选人物映射问题；章节提取到的未关联人物会以候选身份进入下拉选项并参与提示词上下文，不会因缺少作品角色 ID 被丢弃，也不会自动写入角色库。
- 2026-07-02：视觉资产工作台优化为先选图片类型、再选章节的生成路径；高清头像按章节加载角色，章节服饰先选章节角色再选该角色服饰，章节道具不再要求选择角色；提示词生成进一步收紧为纯白背景主体素材，避免背景、色调和场景氛围描述。
- 2026-07-01：视觉资产工作台新增章节内 AI 候选提取，角色、服饰和道具选择按当前章节加载；提示词模板收敛为白底一致性素材；多视角全身图支持正面/侧面/背面方向和模型能力门控下的参考图生成，方向只进入单次生成请求，不污染保存的提示词。
- 2026-06-28：新增作品级视觉资产工作台，支持角色高清面部特写、章节服饰/道具、多视角全身图三类提示词；管理员可配置多生图模型并通过用户级功能权限授权普通用户；图片生成走服务端代理并保存为内部资产引用。
- 2026-06-29：视觉资产新增默认本地 / 可选 Immich 存储模式；Immich 模式按项目名称自动创建或复用相册，生成前先验证服务、认证和上传能力，图片通过后端代理读取；章节提示词拆分为章节服饰和章节道具，旧章节服饰/道具类型继续兼容展示。
- 2026-06-29：生图配置升级为先配置厂商、再启用模型；服务端新增 provider adapter 与安全上游请求 helper，保留 OpenAI Images / OpenAI 兼容路径，并支持 MiniMax 中国 `image_generation` 文生图。
- 2026-06-29：修复合并后生产构建的 TypeScript 严格检查问题，补齐系统 AI 配置数组类型并清理模型管理页未使用声明。
- 2026-06-30：GitHub Docker 构建工作流与测试 Docker 部署脚本支持通过 `NTFY_URL` / `NTFY_TOPIC` 发送完成通知；本地部署脚本在无新提交跳过构建时不发送通知，部署失败会发送失败通知。

## 执行约束

- 所有生图能力必须走服务端代理，浏览器不暴露上游 API Key、真实 provider URL 或自定义 Header。
- 章节相关提示词生成必须使用小窗口、摘要或结构化上下文，禁止单次请求发送或接收整章正文。
- 图片二进制不写入作品 JSON；作品状态只保存提示词、模型快照、生成状态和内部资产引用。
- Immich 服务地址、API Key 和远端读取细节只保存在服务端配置中；普通用户和浏览器只能访问 Story Matrix AI 的同源代理地址。
- Immich 模式必须先通过 readiness gate 再调用生图 Provider；Provider 成功但上传失败时最多自动重试 3 次，全部失败后进入可恢复状态。
- V1 不做额度统计、人脸一致性评分、批量重绘、自动挑图或角色头像/章节插图自动写入。
